defaults:
  - param: ../../met_test/optional_arguments
  - threshold: ../../met_test/threshold/standard
  - _self_

name: "permutation on evaluation"
flag: true
stage: test
# both test

param:
  num_features: 3000