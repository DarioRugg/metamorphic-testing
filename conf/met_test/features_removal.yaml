defaults:
  - param: ../../met_test/optional_arguments
  - threshold: ../../met_test/threshold/standard
  - _self_

name: "features removal"
flag: true
stage: both
# both

param:
  num_features: 1500
