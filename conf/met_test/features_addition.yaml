defaults:
  - param: ../../met_test/optional_arguments
  - threshold: ../../met_test/threshold/standard
  - _self_

name: "features addition"
flag: true
stage: both
# both

param:
  num_features: 2500
